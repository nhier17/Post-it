{
  "version": 2,
  "builds": [
    { "src": "prisma", "use": "prisma" }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/api/(.*)", "dest": "/app/api/$1" },
    { "src": "/(.*)", "dest": "/app/pages/$1" }
  ]
}
