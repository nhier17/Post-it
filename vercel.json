{
  "version": 2,
  "builds": [
    { "src": "prisma", "use": "prisma" }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/api/(.*)", "dest": "/pages/api/$1" },
    { "src": "/(.*)", "dest": "/pages/$1" }
  ]
}
