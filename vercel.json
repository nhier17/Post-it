{
  "version": 2,
  "builds": [
    { "src": "prisma", "use": "prisma" }
  ],
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/app/api/$1" },
    { "source": "/(.*)", "destination": "/app/pages/$1" }
  ]
}
